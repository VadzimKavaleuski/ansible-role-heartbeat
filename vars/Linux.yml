---
default:
  mb_home: /usr/share/heartbeat
  mb_log_dir: /var/log/heartbeat
  mb_conf_dir: /etc/heartbeat
  mb_data_dir: /var/lib/heartbeat

default_ssl:
  key: /etc/pki/tls/private/server.key
  certificate: /etc/pki/tls/certs/server.crt
  certificate_authorities: /etc/pki/CA/ca-root.pem

default_logpath: /var/log/*.log
